// Schema definition for Convex backend
// This file is used by `npx convex dev` to generate the database schema
// For now, it's a reference — run `bun convex dev --once` to activate

export default {};
